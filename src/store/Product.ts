/*
import { ref, computed } from "vue";

export const stateProduct = ref({});

export const setCurrentProduct = (newValue) => {
  return Object.assign(stateProduct.value, newValue);
};

export const user = computed(() => stateProduct.value);
*/
